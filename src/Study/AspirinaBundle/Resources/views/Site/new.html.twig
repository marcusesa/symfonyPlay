{% extends 'AspirinaBundle::layout.html.twig' %}

{% block content -%}
    <div class="page-header">
        <h1>Site creation</h1>
    </div>

    <form action="{{ path('site_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <a href="{{ path('site') }}" class="btn-navbar">Back to the list</a>
{% endblock %}
