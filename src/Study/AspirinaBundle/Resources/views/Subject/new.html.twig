{% extends 'AspirinaBundle::layout.html.twig' %}

{% block content -%}
    <div class="page-header">
        <h1>Subject creation</h1>
    </div>

    <form action="{{ path('subject_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <a href="{{ path('subject') }}">Back to the list</a>
{% endblock %}
