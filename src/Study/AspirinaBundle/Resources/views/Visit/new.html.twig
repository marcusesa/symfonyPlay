{% extends 'AspirinaBundle::layout.html.twig' %}

{% block content -%}
    <div class="page-header">
        <h1>Visit creation</h1>
    </div>        

    <form action="{{ path('visit_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit" class="btn">Create</button>
        </p>
    </form>

    <a href="{{ path('visit') }}" class="btn-navbar">
        Back to the list
    </a>
{% endblock %}
